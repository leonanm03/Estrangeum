export function Objects() {
  // const { getObjects } = useObjects();

  //   const [selectedCategory, setSelectedCategory] = useState(null);
  //   const [objects, setObjects] = useState(null);

  //   useEffect(async () => {
  //     if (selectedCategory) {
  //       const month = layoutMonth - 1;

  //       const queryString = new URLSearchParams({ day, month, year }).toString();

  //       try {
  //         const activitiesData = await getObjects(queryString);

  //         setObjects(activitiesData);
  //       } catch (error) {
  //         alert("Erro ao selecionar o dia das atividades");
  //       }
  //     }
  //   }, [selectedCategory]);
  return (
    <>
      <div>testando</div>
    </>
  );
}
